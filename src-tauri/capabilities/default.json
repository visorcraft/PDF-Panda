{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main application window.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
